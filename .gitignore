# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/

# uv / virtual environments  (uv.lock should be committed)
.venv/
venv/

# Node / web
node_modules/
dist/
# built GUI staged into the package for wheel bundling (run.sh --prod)
src/magnetics/service/webapp/
.vite/
*.local
*.log
*.tsbuildinfo

# Env / local config (VITE_API_BASE, backend hosts, tokens) — never commit
.env
.env.*
!.env.example

# Data caches (reduced shot bundles fetched from MDSplus)
data/cache/
*.nc

# HDF5 shot data — large, fetched/generated locally, not committed
*.h5
*.hdf5
# example shot bundles (extensionless netCDF/HDF5: RAW, PLASMA_PARAMS, COUPLING)
analysis/data/

# OS / editor
.DS_Store
.idea/
.vscode/

# GA-copyrighted source docs — distributed via GitHub Release, not committed
resources/
