# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv / virtual environments  (uv.lock should be committed)
.venv/
venv/

# Node / web
node_modules/
dist/
.vite/
*.local
*.log
*.tsbuildinfo

# Env / local config (VITE_API_BASE, backend hosts, tokens) — never commit
.env
.env.*
!.env.example

# Data caches (reduced shot bundles fetched from MDSplus)
data/cache/
*.nc

# OS / editor
.DS_Store
.idea/
.vscode/

# GA-copyrighted source docs — distributed via GitHub Release, not committed
resources/
