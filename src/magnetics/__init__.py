"""magnetics — device-agnostic 3D magnetic-sensor analysis of tokamak MHD instabilities.

Importable as a plain library (e.g. in a Jupyter notebook): `import magnetics`.
The core analysis pulls only numpy/scipy/matplotlib; the FastAPI service and the
bundled GUI ship in the base install so `pip install magnetics` + `magnetics`
runs the app with no extras. See docs/CONTRACT.md and docs/VISION.md.
"""

__version__ = "0.1.0"
